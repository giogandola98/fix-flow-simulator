import { ScenarioNode } from '../../../types';
export function ExpectFIXConfig({ node }: { node: ScenarioNode }) {
  return <div className="text-xs text-gray-500">ExpectFIX config — stub ({node.id})</div>;
}
