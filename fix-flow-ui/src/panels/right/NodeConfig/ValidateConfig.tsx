import { ScenarioNode } from '../../../types';
export function ValidateConfig({ node }: { node: ScenarioNode }) {
  return <div className="text-xs text-gray-500">Validate config — stub ({node.id})</div>;
}
