import { ScenarioNode } from '../../../types';
export function RetryConfig({ node }: { node: ScenarioNode }) {
  return <div className="text-xs text-gray-500">Retry config — stub ({node.id})</div>;
}
